关联视频：https://www.bilibili.com/BV15MvDBXE5c

提示词：

使用opencv和ffmpeg，完成下列任务：同时读取C0028_mocha_pro.mov和C0028.MP4，它们的帧一一对应，但分辨率不同。要根据C0028_mocha_pro.mov的帧，对后一个视频的帧进行逐帧处理（使用tqdm显示进度），并编码成指定分辨率的视频（全局变量指定，默认为1920*1080）。单帧的处理规则为，将参考帧黑白二值化，此时其为一个黑色背景并且其中有一个白色矩形，获得该矩形的左上角右下角坐标，然后根据两视频分辨率的比例，变换成被处理视频坐标系下的坐标。然后对该选区进行扩展，中心不动，宽度扩展为x倍（也用全局变量，默认2）然后高度根据输出指定分辨率的长宽比算出（例如此处默认是1080/1920*变换后宽度），然后若该区域超出被处理视频的坐标范围，通过平移使其在坐标范围内，则该区域就是被切下的输出区域。然后将其resize到目标大小输出


